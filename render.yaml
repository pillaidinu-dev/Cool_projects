services:
  - type: web
    name: plansync
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    healthCheckPath: /api/health
    buildFilter:
      paths:
        - Dockerfile
        - client/**
        - server/**
      ignoredPaths:
        - nfl-dashboard/**
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: INVITE_CODE
        sync: false

  - type: web
    name: nfl-dashboard
    env: docker
    dockerfilePath: ./nfl-dashboard/Dockerfile
    dockerContext: .
    plan: free
    healthCheckPath: /api/health
    buildFilter:
      paths:
        - nfl-dashboard/**
